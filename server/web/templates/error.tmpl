{{template "header"}}
<div id="mainBody">
    <span>{{.}}</span>
    <hr/>
    <span class="textBold">Possible issues...</span>
    <ul>
        <li>The session ID was mistyped.</li>
        <li>The ACT plugin failed to connect. Try restarting ACT to reset the connection.</li>
        <li>The session expired. Restart ACT to reset the connection and retreive a new session ID.</li>
    </ul>
</div>

{{template "footer"}}